For the `iML1515_EXP` RC training, yes: only `GR_AVG` is the supervised target.

How it is trained:

1. Data split:
- `read_XY(trainingfile, 38)` takes first 38 columns as `X` and next 1 column as `Y` ([Build_Model_RC.ipynb:239](/home/teeruoko/Documents/amn_release/Build_Model_RC.ipynb:239), [Build_Dataset.py:66](/home/teeruoko/Documents/amn_release/Library/Build_Dataset.py:66)).
- In `Dataset_input/iML1515_EXP.csv`, that single target column is `GR_AVG` ([iML1515_EXP.csv:1](/home/teeruoko/Documents/amn_release/Dataset_input/iML1515_EXP.csv:1)).

2. RC architecture:
- A prior dense net is created (`n_hidden_prior=1`, `hidden_dim_prior=500`) ([Build_Model_RC.ipynb:240](/home/teeruoko/Documents/amn_release/Build_Model_RC.ipynb:240), [Build_Model_RC.ipynb:241](/home/teeruoko/Documents/amn_release/Build_Model_RC.ipynb:241)).
- That prior net maps variable medium features to AMN input; fixed medium entries are clamped to reservoir defaults in `input_RC` ([Build_Model.py:851](/home/teeruoko/Documents/amn_release/Library/Build_Model.py:851), [Build_Model.py:861](/home/teeruoko/Documents/amn_release/Library/Build_Model.py:861)).
- The AMN reservoir is loaded and used non-trainably (`trainable=False`) ([Build_Model.py:1508](/home/teeruoko/Documents/amn_release/Library/Build_Model.py:1508), [Build_Model.py:915](/home/teeruoko/Documents/amn_release/Library/Build_Model.py:915)).

3. Loss target used:
- RC output concatenates many things (`Post_outputs, SV, PinV, Vpos, V, Res_inputs`) ([Build_Model.py:935](/home/teeruoko/Documents/amn_release/Library/Build_Model.py:935)).
- But `my_mse` compares only the first `y_true.shape[1]` columns, and for this run `y_true.shape[1]=1` ([Build_Model.py:68](/home/teeruoko/Documents/amn_release/Library/Build_Model.py:68), [Build_Model.py:71](/home/teeruoko/Documents/amn_release/Library/Build_Model.py:71)).
- So training supervises only column 0 (growth), i.e. `GR_AVG`. Constraint terms are output but not directly targeted in RC training.

For the RC model (prior dense net + frozen AMN reservoir), `R2` is computed on the **first output columns only** (the supervised target), not on constraint columns.

1. During training (`fit`), metric is `my_r2`:
- `my_r2 = 1 - SS_res/(SS_tot+eps)` using `y_pred[:, :y_true.shape[1]]`  
[Build_Model.py](/home/teeruoko/Documents/amn_release/Library/Build_Model.py:78)

2. RC output has many columns:
- `[Post_outputs, SV, PinV, Vpos, V, Res_inputs]`  
[Build_Model.py](/home/teeruoko/Documents/amn_release/Library/Build_Model.py:935)

3. Final reported objective R2 after training uses sklearn:
- `evaluate_model()` takes `yt = y_true[:,:end]`, `yp = y_pred[:,:end]`
- For RC, `end = y_true.shape[1]` (no AMN constraint subtraction)
- Then `r2_score(yt, yp, multioutput='variance_weighted')`  
[Build_Model.py](/home/teeruoko/Documents/amn_release/Library/Build_Model.py:1043)  
[Build_Model.py](/home/teeruoko/Documents/amn_release/Library/Build_Model.py:1056)

4. In your iML1515 RC notebook:
- `X,Y = read_XY(...,38)` so `Y` is 1D target (`GR_AVG`)  
[Build_Model_RC.ipynb](/home/teeruoko/Documents/amn_release/Build_Model_RC.ipynb:239)
- They also explicitly compute `r2_score(model.Y, pred[:,0], ...)`  
[Build_Model_RC.ipynb](/home/teeruoko/Documents/amn_release/Build_Model_RC.ipynb:253)

So practically for this case:
`R2 = r2_score(GR_AVG_true, GR_AVG_pred)` where `GR_AVG_pred = pred[:,0]`.

Important caveat: in that cell `xfold=0`, so train and test are the same data (not a held-out test).  
[Build_Model_RC.ipynb](/home/teeruoko/Documents/amn_release/Build_Model_RC.ipynb:242)

`xfold=0` triggers the “no cross-validation” branch, and that branch explicitly reuses the same dataset for both train and test.

Code path:

1. In the notebook, model is created with `xfold = 0`  
[Build_Model_RC.ipynb:242](/home/teeruoko/Documents/amn_release/Build_Model_RC.ipynb:242)

2. In `train_evaluate_model`, condition `if param.xfold < 2` is true  
[Build_Model.py:1196](/home/teeruoko/Documents/amn_release/Library/Build_Model.py:1196)

3. It calls:
`train_model(param, X, Y, X, Y, ...)`  
so `Xtrain==Xtest` and `Ytrain==Ytest`  
[Build_Model.py:1197](/home/teeruoko/Documents/amn_release/Library/Build_Model.py:1197)

4. Inside `train_model`, fitting uses `validation_data=(Xtest, Ytest)`  
[Build_Model.py:1148](/home/teeruoko/Documents/amn_release/Library/Build_Model.py:1148)  
which is the same data as training in this case.

Implication:

- Reported “test” R2 is not out-of-sample generalization.  
- It is an in-sample performance estimate (optimistic).

That is why it’s a caveat: good R2 here means the model fits known points, not necessarily unseen media.  
If you want generalization, use `xfold >= 2` or a true hold-out split.

Yes, for the **main Figure 5 R² (panel c)** it is in-sample.

Why:

1. The paper explicitly says for Fig. 5 that “the whole experimental dataset is used to train the neural layer,” then they extract `Vin` and run FBA on those same conditions ([Faure etal 2023.pdf](/home/teeruoko/Documents/amn_release/Faure etal%202023.pdf)).
2. It also explicitly notes cross-validation results are in **Supplementary Fig. S9** (“uptake fluxes found for cross-validation sets”) rather than main Fig. 5 ([Faure etal 2023.pdf](/home/teeruoko/Documents/amn_release/Faure etal%202023.pdf)).

Repo evidence matches that split:

1. `Q2_train` is high (`~0.974–0.976`): [iML1515_UB_AMN_QP_RC_AMN_Q2_train.csv](/home/teeruoko/Documents/amn_release/Result/Raw_data/iML1515_UB_AMN_QP_RC_AMN_Q2_train.csv)
2. `Q2_pred` (cross-val prediction) is lower (`~0.727–0.787`): [iML1515_UB_AMN_QP_RC_AMN_Q2_pred.csv](/home/teeruoko/Documents/amn_release/Result/Raw_data/iML1515_UB_AMN_QP_RC_AMN_Q2_pred.csv)

So: **Figure 5 main R² is training-set/in-sample; held-out/cross-val performance is reported separately (Supp. Fig. S9).**

Yes. The repo has the Figure 5C generation code in [Figures.ipynb](/home/teeruoko/Documents/amn_release/Figures.ipynb:1096).

For Fig. 5C specifically:

1. Measured sample values come from [Dataset_experimental/EXP110.csv](/home/teeruoko/Documents/amn_release/Dataset_experimental/EXP110.csv:1):
- `GR_AVG` is used as x-values ([Figures.ipynb](/home/teeruoko/Documents/amn_release/Figures.ipynb:1100))
- `GR_STD` is used as horizontal error width `xerr` ([Figures.ipynb](/home/teeruoko/Documents/amn_release/Figures.ipynb:1102), [Figures.ipynb](/home/teeruoko/Documents/amn_release/Figures.ipynb:1148))

2. Predicted y-values for Fig. 5C come from [Result/Raw_data/Cobra_train.csv](/home/teeruoko/Documents/amn_release/Result/Raw_data/Cobra_train.csv:1) column 0 ([Figures.ipynb](/home/teeruoko/Documents/amn_release/Figures.ipynb:1110), [Figures.ipynb](/home/teeruoko/Documents/amn_release/Figures.ipynb:1140)).

3. The horizontal orange lines are the `plt.errorbar(..., xerr=ref_gr_std, ls="none")` call, i.e., from `GR_STD`, not from `Cobra_train.csv` ([Figures.ipynb](/home/teeruoko/Documents/amn_release/Figures.ipynb:1148)).

You can also see those orange horizontal segments in the exported SVG [Figures/Fig5C.svg](/home/teeruoko/Documents/amn_release/Figures/Fig5C.svg:40) (`LineCollection_1`, stroke `#de8f05`).

Cross-validated performance can still be shown as one Q² if they do this:

1. Generate one out-of-fold prediction for every sample.
2. Concatenate those predictions across folds.
3. Compute one global `r2_score` on the full vector.

That is exactly what this codebase does.

- Fold predictions are stitched into one `Ypred` array in [Build_Model.py:1208](/home/teeruoko/Documents/amn_release/Library/Build_Model.py:1208) and [Build_Model.py:1223](/home/teeruoko/Documents/amn_release/Library/Build_Model.py:1223).
- S9 plotting code loads one file (`Cobra_pred.csv`) and computes a single `q2 = r2_score(...)` in [Figures.ipynb:1588](/home/teeruoko/Documents/amn_release/Figures.ipynb:1588) and [Figures.ipynb:1620](/home/teeruoko/Documents/amn_release/Figures.ipynb:1620).

So S9’s `Q2=0.78` is a single aggregate out-of-fold score, not fold-wise mean±std.

You’re right that they *could* report std too. In fact this repo has repeated values in [iML1515_UB_AMN_QP_RC_AMN_Q2_pred.csv](/home/teeruoko/Documents/amn_release/Result/Raw_data/iML1515_UB_AMN_QP_RC_AMN_Q2_pred.csv), but that spread is not what S9 panel itself displays.

Because S9 is computed from a **single prediction vector** (`Cobra_pred.csv`), not from the 3 values in `...Q2_pred.csv`.

- S9 code uses `cobra_train = "Result/Raw_data/Cobra_pred.csv"` and then computes one `q2 = r2_score(...)`  
[Figures.ipynb](/home/teeruoko/Documents/amn_release/Figures.ipynb:1357), [Figures.ipynb](/home/teeruoko/Documents/amn_release/Figures.ipynb:1373)

- `...Q2_pred.csv` stores 3 repeat-level RC scores (different runs/seeds), not the S9 plotted Cobra score.

- In RC notebook, Cobra input is exported from `PRED[0]` (first repeat), not averaged over repeats:  
[Build_Model_RC.ipynb](/home/teeruoko/Documents/amn_release/Build_Model_RC.ipynb:271)

So S9 showing `Q2=0.78` is consistent with plotting one run’s `Cobra_pred.csv` (your local file gives ~`0.786`, typically shown/truncated as `0.78`).  
If they had summarized repeats, it would be around `0.76 ± 0.02` from `...Q2_pred.csv`.

Effectively yes for the released figure: S9 shows a **single selected run’s** Q², not a mean±std over CV repeats/folds.

- The S9 plot computes one `r2_score` from `Result/Raw_data/Cobra_pred.csv` only.
- It does **not** aggregate `iML1515_UB_AMN_QP_RC_AMN_Q2_pred.csv` (which has 3 repeat values).
- In your repo, that single plotted value (`~0.786`, shown as `0.78`) is the top/near-top among those repeat-level values.

So it’s not “best fold,” but it is one chosen run (and numerically it matches the best-looking repeat).
